import 'package:flutter/material.dart';

class Talent_Pools extends StatelessWidget{
  @override
  Widget build(BuildContext context) =>
    Scaffold(
      appBar: AppBar(
        title: Text('Talent Pools'),
        centerTitle: true,
        backgroundColor: Colors.red,
      ),
    );

  
}